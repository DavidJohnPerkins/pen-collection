<%- include('../includes/head.ejs') %>
	<link rel="stylesheet" href="/css/collection-item.css">
	</head>

	<body>
		<%- include('../includes/navigation.ejs') %>

			<main>
				<% if (items.length > 0) { %>
					<div class="grid">
						<% for (let item of items) { %>
							<article class="card map-item">
								<header class="map__header">
									<h1 class="map__title">
										<%= item.BRAND %>
									</h1>
									<h2 class="map__title">
										<%= item.RANGE %>
									</h2>
									<h2 class="map__title">
										<%= item.INK_NAME %>
									</h2>
								</header>
								<div class="pen__image">
									<img src="images/ink-image/<%= item.IMAGE_1 %>" alt="<%= item.INK_NAME %>">
								</div>
								<div class="card__content">
									<p class="map_id">
										Id: <%= item.item_id %>
									</p>
								</div>
								<div class="card__actions">
									<a href="/inkItem/<%= item.item_id %>" class="btn">Details</a>
								</div>
							</article>
							<% } %>
					</div>
					<% } else { %>
						<h1>No Inks Found</h1>
						<% } %>
			</main>
			<%- include('../includes/end.ejs') %>
